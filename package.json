{
  "name": "tg-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --no-fork",
    "dev:reset": "npx nuxi cleanup && nuxt dev --no-fork",
    "cleanup": "npx nuxi cleanup",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.21.4",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  }
}
